import type { LayoutLoad  } from "./$types"; 

// let title: string; 

// export const load: LayoutLoad = async (url) => {  
//     title = await setupTitle(url.url.pathname);
//     return {
//         title: title,
//     }
// }

// async function setupTitle(uri: string) {
//     switch (uri) {
//         case '/register':
//             return 'Create an account'  
//         case '/login':
//             return 'Login'
//         default:
//             return 'Login'
//     }
// }